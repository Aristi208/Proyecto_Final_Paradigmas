@model List<CL_Taller.CVehiculo.Vehiculo>

<h2>Mis Vehículos</h2>

@if (Model.Count == 0)
{
    <p>No tienes vehículos registrados.</p>
}
else
{
    <table>
        <thead>
            <tr>
                <th>Placa</th>
                <th>Marca</th>
                <th>Modelo</th>
                <th>Año</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var v in Model)
            {
                <tr>
                    <td>@v.Placa</td>
                    <td>@v.Marca</td>
                    <td>@v.Modelo</td>
                    <td>@v.Ano</td>
                </tr>
            }
        </tbody>
    </table>
}

<a href="/Factura/MisFacturas">Ver mis facturas</a>